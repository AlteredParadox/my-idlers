<?xml version="1.0" encoding="UTF-8"?>
<!--
    Runs the suite against MySQL/MariaDB instead of SQLite. Production is MySQL,
    and several bug classes (int columns returned as strings, changed-vs-matched
    row counts from ->update()) are invisible under SQLite. Credentials point at
    a throwaway test database (not real data). Run: vendor/bin/phpunit -c phpunit.mysql.xml
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory suffix=".php">./app</directory>
        </include>
    </source>
    <php>
        <server name="APP_ENV" value="testing"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="CACHE_DRIVER" value="array"/>
        <server name="DB_CONNECTION" value="mysql"/>
        <!-- env (not server) so a real DB_HOST environment variable wins:
             Gitea CI runs the job in a container where the MariaDB service
             is reachable at hostname "mariadb", not 127.0.0.1. -->
        <env name="DB_HOST" value="127.0.0.1"/>
        <server name="DB_PORT" value="3306"/>
        <server name="DB_DATABASE" value="my_idlers_test"/>
        <server name="DB_USERNAME" value="idlers"/>
        <server name="DB_PASSWORD" value="idlers"/>
        <server name="EXCHANGE_RATES_URL" value=""/>
        <server name="MAIL_MAILER" value="array"/>
        <server name="MAIL_FROM_ADDRESS" value="test@example.com"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>
        <server name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
